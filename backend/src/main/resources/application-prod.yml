spring:
  datasource:
    password: ${DB_PASSWORD:}
  flyway:
    # 版本化迁移只对生产启用：既有库以 V5 为基线，schema.sql 仅用于全新建库
    enabled: true
    baseline-on-migrate: true
    baseline-version: 5
    baseline-description: pre-flyway production schema
    validate-on-migrate: true
    clean-disabled: true
    out-of-order: false
    locations: classpath:db/migration
  lifecycle:
    timeout-per-shutdown-phase: 30s

server:
  shutdown: graceful

jwt:
  secret: ${JWT_SECRET:}

app:
  cors:
    allowed-origins: ${APP_CORS_ALLOWED_ORIGINS:}

info:
  app:
    image-tag: ${APP_IMAGE_TAG:unknown}

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      probes:
        enabled: true
      show-details: never
      group:
        readiness:
          include: readinessState,db
        liveness:
          include: livenessState
  health:
    livenessstate:
      enabled: true
    readinessstate:
      enabled: true
  info:
    env:
      enabled: true

logging:
  level:
    com.pzhu.eduadmin: info
