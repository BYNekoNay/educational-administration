# 2026-07-17 后端单元测试全量补全

## 背景
项目 159 个源码文件仅 11 个测试文件（6.9%），9/17 Service 已测，0 Controller 测试。

## 完成内容

### Phase 1: 核心缺失服务测试（7 文件）
- AuthServiceMockTest (13 tests) — 登录/注册/暴力破解/BCrypt
- LeaveRequestServiceMockTest (16 tests) — 请假/审核/绑定检查
- NoticeServiceMockTest (8 tests) — 公告CRUD
- ExamServiceMockTest (17 tests) — 考级/报名
- LearningServiceMockTest (13 tests) — 作业/学情记录/批量
- ScheduleServiceMockTest (19 tests) — 排课/教室/调课/批量
- ExportServiceTest (7 tests) — Excel 导出

### Phase 2: RBAC 服务测试（3 文件）
- MenuServiceMockTest (8 tests) — 菜单CRUD/递归删除
- PermissionServiceMockTest (8 tests) — 权限CRUD/级联
- RoleServiceMockTest (13 tests) — 角色/系统保护/物理删除/权限分配

### Phase 3: 安全与通用组件测试（3 文件）
- JwtUtilTest (6 tests) — HS256 签发/解析/过期
- JwtInterceptorTest (11 tests) — Token/拦截/角色鉴权
- GlobalExceptionHandlerTest (13 tests) — 12种异常全覆盖

### Phase 5: 集成测试基础设施
- application-test.yml (H2 内存数据库)
- SmokeTest (2 tests) — 上下文加载

## 结果
- 测试文件：11 → 24
- 测试总数：~164 → 337（第二轮审计后从 318 补至 337）
- Service 层覆盖率：53% (9/17) → 100% (17/17)
- 全量通过：0 失败 0 错误

## 第二轮审计修复（07-17 第二轮）
### P0
- ScheduleServiceMockTest: +15 测试 (updateLesson, checkConflict, getClassroomById, updateClassroom, pageRoomBookings, pageAdjustRequests, auditAdjustRequest 审核通过 3 个, batchCreate 教室/班级冲突/空列表/单元素)
- GlobalExceptionHandlerTest: +1 (handleNoHandlerFound, 完全遗漏的 handler)
- ExportService: 修复 bug — exportSalaries 未校验 month 格式（与 exportPayments/exportLessonFlows 不一致），新增校验逻辑 + 测试

### P1
- JwtInterceptorTest: +1 (handler 非 HandlerMethod 放行)
- RoleServiceMockTest: +1 (updateRolePermissions 空列表 — 只删不建)

## 第三轮审计修复（07-17 第三轮）
- JwtInterceptorTest: +2 (writeJson JSON格式验证、403响应格式验证)
- AuthServiceMockTest: +1 (锁定过期>15分钟自动解锁 — 反射测试)
- ExportServiceTest: +1 (无日期限制导出)
- 总计：337 → 346

## 第四轮审计修复（07-17 第四轮）
- NoticeServiceMockTest: +5 (IP 提取 5 条回退路径全覆盖: X-Forwarded-For、X-Real-IP 回退、remoteAddr 兜底、多 IP 取首个、空字符串回退、RequestContextHolder null)
- 总计：341 → 346

## 第五轮审计修复（07-17 第五轮）
- ExportServiceTest: +3 (响应头验证 Content-Type/Content-Disposition + NPE 修复)
- ExamServiceMockTest: +2 (关键字搜索 keyword、排序字段 sortField)
- 总计：346 → 351

## 最终汇总
- 今天 4 轮审计 + 3 轮补全，测试从 164 → 351 (+187)
- 24 个测试文件，Service 层 17/17 全覆盖

## 第七轮：审计报告问题修复（07-17）
- 修复审计报告 docs/audit-backend-logic-2026-07-17.md 全部 9 项问题
- P0: SalaryServiceImpl CAS原子更新确认/作废、maxStudentCount NPE三处null兜底
- P1: EnrollmentServiceImpl加学员/课程存在校验、迟到扣课、SQL方言改用Page、请假日期校验
- P2: 薪资冗余赋值删除、考勤覆盖请假防护、迟到扣课与到课率口径一致
- 5 个测试文件同步更新（Enrollment/Salary/Student ServiceMockTest）
- 351 测试全量 0 失败
- 351 测试全量 0 失败

## 第十二轮：Attendance 考勤模块深度检查 (07-17)
- 按四维度检查 L2-6 Attendance/LeaveRequest 模块
- P0: reverseDeduct 未覆盖 status=2(迟到) 回冲 — 已修复
- P1: leaveRequest.audit 改用 CAS 原子更新
- P2: reverseDeduct 同步回冲 totalLessons
- 报告：docs/audit-attendance-2026-07-17.md
- 351 测试全量 0 失败

## 第十四轮：L3辅助系统 + L4跨层数据字典一致性 (07-17)
- 全量 status 字段数据字典审查，覆盖 10 个实体
- 修复 4 个实体补充 status 注释：Enrollment(6=已退费)、Student、ExamSignup、ScheduleAdjustRequest
- TeacherSalary.status=3(已发放) 标记为预留未实现
- 351 测试全量 0 失败

## 第十三轮：Student/Course/Schedule/Enrollment/Exam 批量检查 (07-17)
- 批量完成 5 个模块 L2-1~L2-4 + L2-8 四维度检查
- 正面确认：Course/Class cascade delete 完整、Exam signup 去重+校验、Enrollment 定时任务正确
- P2：Student delete 缺考级记录检查 → 已添加 ExamSignupMapper + 检查

## 第十一轮：Finance/Salary 模块深度检查 (07-17)
- 按四维度检查 L2-5 Finance + L2-7 Salary 模块
- 发现 P0×3：退费 totalLessons 未扣减、退费后 enrollment 未更新(可重复退费)、薪资未统计迟到
- 发现 P1×3：createPayment 状态 CAS、退费流水一致性问题
- 发现 P2×3：ClassStudent 恢复无保护、SalaryRule 默认顺序、remark 硬编码
- 报告：docs/audit-finance-salary-2026-07-17.md
- 351 测试全量 0 失败
- 设计了四层递进式检查计划，覆盖 15 个模块 + 1 个跨层集成
- 每层 4 维度：链路完整性、边界与异常、数据一致性、性能风险
- 执行顺序：L2-1~L2-8 核心业务 → L3-1~L3-3 辅助系统 → L4 跨层集成
- 计划文档：docs/backend-audit-plan-2026-07-17.md

## 第九轮：安全/DTO/配置层深度审查 (07-17)
- 审查安全层(JWT/拦截器/TLHolder)、DTO层、定时任务、异常处理
- 修复 P0: WebMvcConfig /api/auth/** 绕过回滚、JWT密钥填充弱化
- 修复 P1: User.password @JsonIgnore、CreateUserRequest @Size、UpdateUserRequest 校验、profile排除password
- 修复 P2: GlobalExceptionHandler 信息泄露(类型名/URL)、@Transactional rollbackFor×10
- 351 测试全量 0 失败
- 审查 22 Controller + 33 Entity + 配置层，发现 30+ 新问题
- P0: BlockAttackInnerInterceptor防全表更新、Enrollment/ScheduleAdjustReq并发审核CAS、FinanceController @Valid×4
- P1: UserController密码空值/状态范围校验、LeaveRequest audit状态校验、StatisticsController类型转换安全
- 351 测试全量 0 失败，2个测试文件Mock同步更新

## 第六轮：全量后端逻辑审查（07-17）
- 审查 17 ServiceImpl + 22 Controller + 33 Entity
- 报告：docs/audit-backend-logic-2026-07-17.md
- 发现 P0×2（薪资竞态/maxStudentCount NPE）、P1×4（报名校验/迟到扣课/SQL方言/请假日期）、P2×3（到课率口径/冗余代码/考勤覆盖）

## 第十五轮：移动端家长学员加载问题（07-17）
- parent1(刘家长,id=7) 在 parent_student 表绑定 student_id=1,12，但 student 表里这2人都 is_deleted=1
- 操作日志显示：student 1 在 13:52/14:15/14:16 三次软删，student 12 在 17:31 软删
- 根因：`/api/parent/students` 用 `studentMapper.selectBatchIds(studentIds)`，触发 @TableLogic 把软删学员过滤掉
- 复现：POST /api/auth/login(parent1/123456) → GET /api/parent/students → 返回 `data:[]`
- 修复方案待定：① 数据修复 UPDATE is_deleted=0；② 代码修复 绕过 @TableLogic；③ 级联软删 parent_student
- 同时修移动端 StudentSwitcher 占位：① home 加"我的孩子"section-header；② '学员'→'暂未添加孩子'/'请选择孩子'

## 第十六轮：移动端功能增强（07-17 晚）
### 家长端功能完善
- StudentSwitcher: "学员"→"暂未添加孩子/请选择孩子"；home 加"我的孩子"section-header
- 报名课程页 (enrollment.vue): 类别简介 + 开班数徽标；展开班级列表含教师/课次摘要"周三/周五 19:00-20:30"/剩余名额/起课日期
- 新增 `GET /api/parent/courses/{courseId}/classes` + ParentClassVO (一次性查教师/人数/课次聚合)
- 报名去重: `.in(2,3)` → `.notIn(4,5)` (覆盖待审核)
- 新增 `GET /api/parent/enrollments/check/{studentId}/{courseId}` → 课程卡打"已报名"徽标
- 新增 `POST /api/parent/payments` (mock 缴费) - 走 FinanceService.createPayment，自动入账/入班
- 缴费时 lessonCount/amount 从课程表回填 (`selectByIdIncludeDeleted` 兜底)
- 课表 (schedule.vue): 按课程分组 + 课程名/班级/教师/教室全名 + 按日期动态判断状态(今日/待上课/已完成)
- 新增 ScheduleLesson.courseId 字段（前端按此分组）+ `ClassGroupMapper.selectCourseNamesByIdsIncludeDeleted`
- 新增 ScheduleLesson.leaveStatus 字段 + LeaveRequestMapper + `getStudentSchedules` 查请假状态
- leave 已通过课表虚化显示"已请假"（虚线+半透明），请假中显示"请假中"

### 缺表修复
- schema.sql 缺 `leave_request` 表（实体已存在但 DDL 漏写）→ 已补 DDL + 重新合并到 reset.sql

### 调试/部署
- PowerShell 不能用 `-Dspring-boot.run.arguments=--server.port=8080` 这种空格形式，要用 `"-Dspring-boot.run.arguments=--server.port=8080"` 整段引号
- `mvn spring-boot:run` 必须从 backend/ 目录执行，根目录没有父 pom
- 微信小程序需要导入 `dist/build/mp-weixin/`，不是项目根
- reset.sql 改为自包含（resetHeader + schema + data 三合一），单文件直灌
