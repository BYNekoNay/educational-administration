# 2026-07-13 工作日志

## 项目架构全面了解
- 用户要求全面了解 educational-administration 项目的整体架构和技术栈
- 系统探索了三个子系统（backend/admin-web/mobile-uniapp）的目录结构、技术栈、配置和代码组织
- 阅读了核心文档（README、项目.md、开发指南、数据库规范、脚手架规范、部署计划等）
- 输出了一份完整的架构分析报告，涵盖：项目背景、技术栈全景、目录组织、数据模型、API路由、配置管理、构建部署、编码规范

## 管理后台全流程功能测试
- 启动后端 Spring Boot 服务（端口 8080）：编译通过，启动成功，MySQL 连接正常
- 启动 admin-web 前端 Vite Dev Server（端口 5173）：启动成功，页面正常渲染
- 通过 API 层面进行全面测试：
  - ✅ 认证模块：登录、Token 签发、用户资料获取
  - ✅ 用户与权限：用户 CRUD、角色管理、权限列表、菜单树
  - ✅ 教务模块：课程/班级/学员/教室/报名/排课/考勤/考级 CRUD
  - ✅ 财务模块：缴费/退费/课时账户/课时流水/薪资规则/薪资结算
  - ✅ 系统管理：机构配置、公告管理、操作日志、运营看板
  - ✅ 角色权限隔离：教师越权拦截正确，多角色接口正常
- 发现问题：JSON 中包含中文字符时 curl 出现编码问题（非后端 Bug），课程创建/学员创建需 ASCII 名称
- 测试数据清理完成

## 项目测试 - 第二轮（java -jar 持久运行）
- 使用 `java -jar target/eduadmin.jar` 替代 `mvn spring-boot:run`，进程持久运行不再因超时被kill
- 修复了 `SERVER__PORT` 环境变量导致的端口覆盖问题（系统环境变量会覆盖 application.yml）
- 使用 Bash `&` 后台启动 admin-web，同样解决了持久运行问题
- 运行了 33 项 API 测试：
  - ✅ 28 项全部通过
  - ❌ 发现问题：
    1. 创建课程/教室 POST 返回 409 数据冲突（演示数据已存在同名记录/唯一约束冲突）
    2. 创建用户 POST 返回 400 用户名不能为空（正常参数校验，body `{}` 缺字段）
    3. ⚠️ **Dashboard 看板未限制财务角色访问** - `DashboardController` 可能缺少 `@RequireRole` 注解
    4. 无 token 请求返回 401 而非 HTTP 401 错误（功能正常，属于规范响应）

## 执行修复（批次①-④）
- 按计划执行 4 批次修复，全部完成：
  - ✅ F1: DashboardController 加 `@RequireRole({"SUPER_ADMIN","EDU_ADMIN"})` — FINANCE 被拦截
  - ✅ F3: JwtInterceptor `setStatus(200)` → `setStatus(code)` — 无token返回 HTTP 401
  - ✅ F2: Course.java 加 `@NotBlank/@NotNull`；Classroom.java 加 `@NotBlank/@Positive`
  - ✅ F4: CourseServiceImpl.createCourse() 和 ScheduleServiceImpl.createClassroom() 加 Service 层前置校验
  - ✅ F5: GlobalExceptionHandler 已存在 MethodArgumentNotValidException 处理（无需修改）
  - ✅ 前端兼容：Axios 拦截器已同时处理旧格式(data.code=401)和新格式(HTTP 401)（无需修改）
- 最终验证：Dashboard 限权生效 ✅ / HTTP 401 正确 ✅ / 参数校验返回 400 ✅ / 全部回归通过 ✅
- 基于测试结果设计了 5 项修复方案（F1-F5），分为 P0-P3 四个优先级
- 按"安全→数据完整性→一致性"分 3 批实施，第 4 批验证
- 涉及 8 个文件，包含后端 Java 和前端 TypeScript
- 计划文件写入：`electric-thunder-lovelace.md`

## 移动端 uni-app 测试
- 初始化 uni-app CLI 项目（创建 vite.config.ts、更新 package.json、npm install 445 包）
- 启动 H5 开发服务器在 5174 端口
- **API 测试结果：13/15 通过**
  - ✅ 家长端：登录、学员列表、课表(6条)、考勤(4条)、学情(1条带lessonId)、课程(13个)、通知、在线报名
  - ✅ 教师端：登录、课次(9条)、课次学员名单(2人)、考勤记录、作业列表(正常空)
  - ❌ 课时账户 500 — `lesson-account` 端点在后端未实现（URL 路由缺失）
  - ⚠️ 学情记录 — 前端调用时未传 `lessonId` 参数，需修复前端调用

## 修复移动端 3 个问题
- ✅ Issue 1（P0）：在 AttendanceController 中新增 `/api/parent/students/{id}/lesson-account` 端点，注入 FinanceService 查询课时账户 → 验证: 返回 2 条数据
- ✅ Issue 2（P1）：LearningController 将 `lessonId` 改为 `required=false`，新增 `LearningService.getRecordsByStudentId()` → 验证: 无lessonId返回4条/带lessonId返回1条
- ✅ Issue 3（P2）：教师端 4 个页面（attendance/learning/schedule/statistics）全部替换为 `import { api } from '@/utils/request'`，删除硬编码的 baseUrl 和 token 拼接；statistics.vue 同时修复 `@/vue-compat` 错误路径和 `/api/admin/dashboard` 权限问题
- 涉及 8 个文件（后端 4 + 移动端 4），全部验证通过

## 按文档完善项目（Phase 1 + 2）
- ✅ Phase 1: M4 考勤状态码修正 (docs/05 line 91) / m2 密码环境变量化 / C2 Redis 注释说明 / 审计报告 C1/C3/M1/M5 标记已修复
- ✅ Phase 2: M6 AttendanceController 拆分为 Admin/Teacher/Parent 三个独立控制器（符合终端隔离原则）/ m1 sys_menu+organization 补充至 docs/10 / m4 SalaryController 新增 DELETE salary-rules + /calculate 路径 / m3 docs 路径引用修正
- 涉及 12 个文件（后5+文6+删1），编译通过，端点验证通过
- Phase 3（前端组件化+移动端补齐+NoticeService）作为后续迭代

## 端口修正：H5 移动端独立端口
- 用户反馈：5174 实际是 admin-web（教务管理平台），不是移动端 H5
- 根因：之前两套 Vite 进程都尝试用 5174 端口，admin-web 占用了 5174
- 修复：
  - admin-web 保持 5173 端口
  - 移动端 H5 改用 5175 端口（mobile-uniapp/vite.config.ts 修改）
  - 创建 src/ 目录并复制 manifest.json + pages.json（uni-app 要求）
- 端口分配：
  - 后端: 8080
  - admin-web: 5173
  - mobile-uniapp H5: 5175

## 移动端全面测试与 P0 修复
- 探索了全部 12 个页面，发现 6 个 P0 级 Bug：
  - ✅ enrollment.vue: 硬编码 localhost → 改为 `api()` 封装；studentId=1 → 改为动态
  - ✅ learning.vue: homeworks 从未加载（空 Tab）；静默 `catch{}` → toast 提示
  - ✅ lesson-account.vue: flows 从未加载 → 增加 flows API 调用 + catch toast
  - ✅ statistics.vue: 到课率硬编码 85 → 真实计算；trends 改为客户端聚合
- **P0 修复后运行 18 项 API 测试：18/18 全部通过**（含登录、家长7项、教师5项、边界3项）
- 移动端 Vite 代理已验证正常工作
- 到课率现基于实际考勤数据计算（取本月前5节已完成的课次）

## UI 设计系统重塑 V2.0
- 按用户要求设计全新界面：**专业·温暖·艺术**的品牌调性 + **鲜明品牌风** + **深度定制主题**
- 创建设计上下文文件 `.impeccable.md` 包含 5 条设计原则 + WCAG AA 对比度合规表
- 建立完整 Design Token 系统（CSS 变量）：
  - 品牌色系：Deep #0C4A6E → Primary #0E7490 → Bright #06B6D4
  - 暖色调点缀：Warm #F97316 / Rose #E11D48 / Amber #F59E0B
  - 温暖中性色 **WCAG AA 合规**：10 级灰阶全部 ≥4.5:1 对比度
  - 排版：PingFang SC 系，Perfect Fourth (1.333) 比例，8 级字号
  - 间距：4px 基数的 12 级系统
  - 圆角：6→10→16→24px 四级
  - 阴影：温暖棕调替代冷灰黑
- 创建 `admin-web/src/styles/tokens.css` + `global.css`
- 重写 `Login.vue` 为左右分栏品牌面板设计
- 覆盖 Element Plus 全套主题变量（色板/边框/圆角/阴影/字体）
- 侧边栏、表格、按钮、输入框全面品牌化
- 修复对比度问题：placeholder 从 #A89B92 (2.84:1 ❌) → #7C6C61 (4.70:1 ✅)；次要文字 #66584E (6.5:1)；正文 #4D4139 (9.5:1)；侧边栏文字 80% 白 (6.5:1)
- admin-web 已重启运行在 5173 端口

## 全局错误提示完善
- 后端修复 5 项（GlobalExceptionHandler/JwtInterceptor/AuthService）：
  - extractRootMessage 运算符优先级 + 括号
  - BusinessException 增加 log.warn 日志
  - ConstraintViolationException 消息清洗（提取 violation 而非暴露技术路径）
  - AuthService 区分"用户名不存在" vs "密码错误"
  - JwtInterceptor 区分 ExpiredJwtException vs 其他 token 错误
- Admin-web 修复 6 项：
  - SalaryList.vue：修复薪资规则保存静默吞错 → 改为 showError
  - MenuManagement.vue：catch null → catch (e)
  - Login.vue：添加 catch 块显示后端错误消息
  - 4 处 catch {} 添加 console.warn
- Mobile-uniapp 修复：
  - 新建 utils/error.js 统一错误工具（getErrorMessage+showError）
  - request.js 增强：导入 error.js，添加非401状态的通用错误处理
  - notices/statistics/login 3处静默 catch 添加 console.warn
- 验证结果：
  - ✅ "用户名不存在" / "密码错误" 区分生效
  - ✅ Token 无效 → HTTP 401
  - ✅ 参数校验 → 具体字段消息（如"价格不能为空"）

## 移动端 UI 美化 V3.0
- 创建移动端共享设计系统 (`styles/tokens.vars` + `styles/content.css`)
- 重写登录页：暖色背景 + 品牌渐变头像 + 圆角输入组 + 品牌色按钮 + 标签化演示账号
- 重写家长首页：头像区 + 5 张功能卡片带彩底图标+描述文字 + 按压缩放反馈
- 重写教师首页：暖橙头像区 + 4 张功能卡片（教师版配色差异化）
- 统一内容页卡片风格（课表/课时/报名）：白色卡片+左蓝色装饰条+结构化信息行+品牌色状态标签
- 设计规范：页面背景 #F5F0ED / 卡片 #FFF / 标题 #362C26 / 正文 #4D4139 / 品牌蓝渐变按钮
- 教师端使用暖橙系头像渐变区分家长端的蓝青系
- API 验证全部通过
