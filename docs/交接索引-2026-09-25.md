# 交付交接索引（2026-09-25）

> 目的：把「下一次可直接推进」所需的一切集中到一份文件里——现状、已完成的改动、待你决策的事项、模板到位后的执行步骤、素材位置、环境命令、遗留风险。
> 生成者：项目总监（MVP 开发专家团）｜ 协作成员：产品经理、前端工程师、后端工程师、测试工程师

---

## 一、当前状态（TL;DR）

| 维度 | 状态 |
|---|---|
| 代码 | **冻结**。`HEAD = 5cc3135`，工作区**无任何源码改动** |
| 版本号 | `backend/pom.xml`、`admin-web/package.json`、`mobile-uniapp/package.json`、`mobile-uniapp/src/manifest.json` 已统一为 **1.3.0**（此前停留在 0.9.0） |
| 回归 | 后端 **464 用例全绿**（0 失败/0 错误/0 跳过，42.3s）；管理端 **26 文件 / 118 用例全绿** + `npm run build` 通过；移动端 4 文件 / 25 用例 |
| 数据库 | **37 张表**，Flyway 迁移 V6 / V7 / V8（schema 版本 8） |
| 文档 | 论文（`docs/thesis/`）、课程设计报告（`docs/paper/`）、开题报告（`docs/开题报告.md`）、答辩材料（`docs/defence/`）**均已成型** |
| **唯一空白** | **答辩 PPT**——学校模板未下发，本轮只做内容层，不排版 |

---

## 二、本轮（2026-09-25）完成的事

### 1. 版本号对齐
`backend/pom.xml:16`、`admin-web/package.json:4`、`admin-web/package-lock.json:3,9`、`mobile-uniapp/package.json:3`、`mobile-uniapp/package-lock.json:3,9`、`mobile-uniapp/src/manifest.json:5-6` 由 `0.9.0` → `1.3.0`；移动端 `versionCode` 同步 `100` → `130`（否则 `versionName=1.3.0` 配 `versionCode=100` 会自相矛盾）。
> ⚠️ 过程提示：移动端这三处**最初漏派了任务**，是在提交前核对 `git status` 时才发现并补上的。**教训：分派"统一版本号"这类跨模块任务时，必须逐文件点名到具体负责人，不能写"由其他成员处理"**——否则会出现"汇报为已完成、实际无人做"。
影响面已排查：`<finalName>eduadmin</finalName>` 使产物名固定为 `target/eduadmin.jar`；部署脚本与 Compose 用 **git SHA** 作镜像标签，不依赖应用版本；无任何代码或配置读取 Maven 版本（`@project.version@` / `build-info` 零命中）。**改版本号不影响部署链路。**

### 2. 修复本地环境（重要，曾导致构建门禁失效）
`admin-web/node_modules` 里的 `@element-plus/icons-vue@2.3.2` 被**截断**：只有 73 个文件（完整包 310 个），缺 `dist/types/index.d.ts` 与 `global.d.ts` 两个类型入口，导致 `vue-tsc` 报 12 处 `TS7016`、`npm run build` 失败。
- **非上游缺陷**：下载 2.3.0 / 2.3.1 / 2.3.2 三个 tarball 逐个校验，**全部包含**该入口文件
- **修复**：`npm ci` 重装（按锁文件，未改依赖版本、未加 shim、未动 tsconfig、未摘掉 `vue-tsc`）
- 重装后 `npm run build` 退出码 0，`vitest` 118/118 通过

> 注：`npm ci` 在本机会被 safe-delete 拦截（一次删 5031 个文件 > 5000 阈值），需先把旧 `node_modules` **改名移走**再装。

### 3. 答辩材料内容层（`docs/defence/`）
| 文件 | 体量 | 内容 |
|---|---|---|
| `答辩讲稿.md` | 15 分钟版 + 5 分钟版 + 数字出处表 | 开场用真实痛点切入（点名表/课时扣错/教室撞时段），非「各位老师好…」套话 |
| `演示动线脚本.md` | 五角色 42 步 + 出错预案 E1~E9 + 截图核对清单 | 每步含点击路径、预期界面、一句话解说、对应截图 |
| `答辩问答预案.md` | 19 问（技术选型 4 / 核心难点 5 / 工程 3 / 局限反思 4 / 工作量 1） | 每题含考察意图、可直接口语化的回答要点、佐证材料 |

### 4. 口径纠错（本轮价值最高的部分）
撰写者自检看不出、必须对着代码逐行比才会暴露的问题，共修正 4 类：

| 问题 | 事实 | 依据 |
|---|---|---|
| 「冲突检测四维」 | 实为**五个时间交叉维度**（教师/教室/教室预约/班级/跨班学员） | `ScheduleConflictServiceImpl.java:58/62/66/72-85/87-146`；与本仓 `docs/12:33` 一致，但与论文正文矛盾——已全库统一 |
| 「余额不足标记欠费」 | **无欠费机制**；余额为 0 时到课**被拒绝**（409） | `AttendanceServiceImpl.java:568-570`、`:608`；全后端搜 `欠费/debt/arrears` 零命中 |
| 「容量校验属冲突检测」 | **不属于**；且**手动排课完全不校验容量**，仅智能排课选教室时过滤 | `ScheduleServiceImpl.java:466-471`（仅此一处） |
| 报名记录状态名 | 实际为 `全部/待审核/待缴费/已完成/已拒绝` | `mobile-uniapp/.../enrollment-history.vue:74-75` |

另精确化 3 处：薪资只计**已完成且有到课/迟到考勤**的课次（请假/缺勤不计薪）；流失预警 5 因子中 **F5 异常出勤默认关闭**；报名快照是**版本令牌 + 指纹校验**（非结果缓存）。

### 5. 连带修复
- `docs/paper/figures/fig2-4.mmd` + `fig2-4.png`：图源由「四维冲突检测/教师·教室·班级·学员」改为「五维冲突检测/教师·教室·教室预约·班级·跨班学员」并**重渲染**（白底 2 倍缩放，与其余 8 张同参数），已目视核验图内文字无截断。此图**同时供** `docs/paper` 图 2.4 与 `docs/thesis` 图 3.4 使用。
- `docs/开题报告.md` 使用说明表字数回填：字段 2 `804→814`、字段 3 `1164→1170`（字段 1 仍 1796）
- `docs/12-核心算法设计文档.md:47`、`docs/10-数据库规范.md:155`、`docs/05-详细设计说明书.md:336`、`docs/05-详细设计说明书.md:81`：补「`schedule_lesson.status` 的 3（已取消）为 schema 预留值，当前课次取消走逻辑删除（`ScheduleLesson.isDeleted`），无代码写入 status=3」的澄清。

> **该问题的波及面已核实清楚**（测试工程师独立逐文件扫描）：不准确的「已取消（3）」表述**只存在于设计文档层**（`docs/12`、`docs/05`）。**交付物层一处都没有**——`docs/thesis`（02/04/05）、`docs/paper`（02/03）、`docs/开题报告.md` 全篇未提该规则；`docs/defence/答辩问答预案.md:99` 反而给出了正确口径。**故不影响论文与答辩。**
> 支撑事实：`sql/schema.sql:225` 定义了 `3-已取消`（定义存在）；全后端对课次 status 的**唯一写入**是 `ScheduleServiceImpl.java:822` `.set(status, 4)`（已调课）；`sql/data.sql` 的 `schedule_lesson` 种子数据 status 只有 1（208 行）与 2（24 行），无 3、无 4。

---

## 三、需要你决策或补充的 4 件事

| # | 事项 | 说明 |
|---|---|---|
| 1 | **开题报告是否需要重贴** | 字段 2、字段 3 正文各改了一处（「四个维度」→「五个时间交叉维度」）。**若已粘进学校在线表单，需重贴对应两个字段** |
| 2 | **论文封面信息** | 学号、院系全称、指导教师姓名、日期——`docs/thesis/00-封面与摘要.md` 对应位置仍为占位符 |
| 3 | **论文待补截图** | 移动端 v1.3 四个新增页面（教师请假审批 / 课表 Tab 真实化 / 消息未读角标 / 家长端学情上下文）**无专用截图**，现场只能实时演示；`docs/thesis` 第 4 章 §4.7 与流失预警面板同样缺图 |
| 4 | **答辩 PPT 模板** | 模板下发后需：① 把讲稿的「建议第 X 页」**重新映射**为真实页序；② 补配图；③ 按模板改版式。**不要照搬现有页码建议** |

---

## 四、模板到位后的执行步骤

### A. 答辩 PPT
1. 以 `docs/defence/答辩讲稿.md` 为内容源，按模板页序重排（讲稿里的页码是内容层建议，**必须重映射**）
2. 配图取自第五节素材索引；移动端四页无图 → 标注「现场实时演示」
3. `docs/defence/演示动线脚本.md` 用于排练，不需要进 PPT
4. `docs/defence/答辩问答预案.md` 单独打印/自留，**不进 PPT**

### B. 论文 docx —— 已重生成，仅剩两项收尾
**2026-09-25 已用 `md2docx.py` 重生成** `docs/thesis/艺术培训机构全流程教务管理平台设计与实现.docx`：结构未变（454 段落 / 16 表格 / 13 节 / 24 内嵌图片），「四维」「四个维度」残留由 3+2 → **0**，「五维」出现 3 处。

**剩余两项**：
1. 封面信息待补（学号、院系全称、指导教师、日期）
2. 目录页码需在 Word 内「引用 → 目录」生成

**工具链已就绪**：`.workbuddy/tmp/md2docx.py`（20KB，**路径硬编码指向 `docs/thesis/` 与论文输出名**，含页眉页脚/页码域/三线表/大纲级别/中文字体处理）。运行方式：
```
"/c/Users/lby0403/.workbuddy/binaries/python/envs/default/Scripts/python.exe" .workbuddy/tmp/md2docx.py
```
（`python-docx` + `pillow` 装在 venv `~/.workbuddy/binaries/python/envs/default`）
学校写作规范原文提取件在 `.workbuddy/tmp/writing-spec.txt`。

### C. ⚠️ 课程设计报告 docx 仍是旧版（**尚未处理**）
`docs/paper/艺培通课程设计报告.docx`（mtime 2026-09-07）内容为旧口径：含「**移动端 6 个**」「**四个维度**」×2，缺 69 / 25 / 五维。
**它的 md 源（`docs/paper/01`、`04`）已于 09-11 更新**，故 docx 与 md 不一致。
**没有现成脚本**——`md2docx.py` 是专为毕设论文写的（硬编码 `docs/thesis/`），**不适用于课程设计报告**（版式要求也不同，应遵循任务书的《课程设计报告示例》体例）。需在排版阶段另写或改造脚本。
> 影响面：若对外声称"全库口径零残留"，这一份是例外。

排版要点（已在该脚本中实现）：页边距上 3 / 下 2.5 / 左右 3 cm；页眉「攀枝花学院本科毕业设计（论文）」+ 章名（段落下边框实现下划线）；页脚居中页码域；前置罗马数字、正文阿拉伯数字从 1；标题三号黑体居中 / 小三黑体居左 / 四号黑体居左；正文小四宋体固定行距 20 磅首行缩进 2 字符；md 表格转三线表。

### C. 开题报告 docx（可选）
表单可直接粘贴纯文本，附件非必需。若要出 docx，同样可复用 `md2docx.py`。

---

## 五、素材索引

### 截图（共 54 张，全部已验证存在）
| 位置 | 数量 | 内容 |
|---|---|---|
| `docs/screenshots/` | 27 | 管理端各页面（运营看板/排课管理/大课表/薪资管理/退费管理/课时流水/课时账户/考级管理/教室管理/机构配置/操作日志/用户管理/角色管理/菜单管理/公告管理等） |
| `docs/screenshots/interactive/` | 12 | 交互态（搜索、新增对话框、Tab 切换、树形结构、状态筛选） |
| `docs/evidence/2026-07-20/` | 6 | 五角色首页与看板（超管/教务/财务看板 + 家长/教师首页 + 家长退费） |
| `docs/paper/figures/` | 9 | Mermaid 渲染图 fig2-1 ~ fig2-9（白底 2 倍缩放） |

### 文档资产
| 路径 | 内容 |
|---|---|
| `docs/00 ~ 16` | 工程文档 17 篇（可研/需求/概设/详设/测试/接口/数据库/算法/综合），对应毕设任务书 |
| `docs/thesis/` | 毕设正式论文：11 个 md（编号=装订顺序）+ docx |
| `docs/paper/` | 课程设计报告体例：7 md + docx + `figures/` |
| `docs/开题报告.md` | 开题报告 4 字段 + 参考文献附件 + 待确认项 + 生成记录 |
| `docs/defence/` | 答辩材料内容层三件套 |
| `docs/deployment-2026-09-10.md` | 部署手册（⚠️ 含生产 IP 与凭据路径，**不要投屏**） |
| `docs/ui-test-report-final-2026-09-10.md` | 浏览器 AI 验收 69/69，含三次重大发现 |
| `docs/acceptance-matrix-2026-07-19.md` | 五角色权限验收矩阵（32 授权 + 6 越权） |
| `docs/plans/`、`docs/reviews/`、`docs/dogfood-reports/`、`docs/ideation/` | 计划、评审、自用测试报告、优化设想 |

### 可复用脚本
| 路径 | 用途 |
|---|---|
| `.workbuddy/tmp/md2docx.py` | md → docx（按学院规范版式），已在毕设论文上验证 |
| `.workbuddy/tmp/writing-spec.txt` | 学院写作规范 PDF 的文本提取件 |

---

## 六、环境与命令速查

| 事项 | 命令 / 说明 |
|---|---|
| 后端测试 | PowerShell `cd backend; mvn.cmd -o test`（离线，约 42s）。**Git Bash 下直接跑 `mvn` 会报 classworlds 找不到主类**（Windows 版 Java 不认 POSIX 路径），须用 `mvn.cmd` 或手工构造 classworlds 启动命令 |
| 后端构建 | PowerShell `cd backend; mvn.cmd -o clean package -DskipTests` → `target/eduadmin.jar` |
| 管理端 | `cd admin-web && npm run build`（含 `vue-tsc` 类型检查）/ `npx vitest run` |
| 移动端 H5 | `cd mobile-uniapp && npm run build:h5`，产物 `dist/build/h5-release` |
| Mermaid 渲染 | 见技能 `mermaid-render-local`：装到 `~/.workbuddy/binaries/node/workspace`，复用本机 Chrome 设 `PUPPETEER_EXECUTABLE_PATH`，用 **Windows 路径**调用 `cli.js`，参数 `-b white -s 2` |
| 沙箱 bash | PATH 为空，每条命令最前面需 `export PATH="...PortableGit/.../usr/bin:...:/usr/bin:/bin:$PATH"` |
| 构建前 | safe-delete 会拦截 `vite`/`mvn` 清目录 → 先把旧目录**改名移走** |

---

## 七、遗留风险（建议后续处理）

| # | 风险 | 影响 | 建议 |
|---|---|---|---|
| 1 | **CI 不跑 `npm run build`** | `.github/workflows/deploy.yml:56` 只有 `npm ci && npm test`（vitest），**类型检查从未进入 CI**；生产镜像走 `Dockerfile.web-lite`（只 COPY 制品）也绕过了这一步。两处叠加导致本次的 `vue-tsc` 失败长期无人发现 | 在 CI 补一个 `npm run build` 步骤；发布前本地至少跑一次完整 `npm run build` |
| 2 | **`node_modules` 曾被抓包残缺** | 原因未定位（截断发生在 09-06 前后）。若再次发生会以 `TS7016` 形式暴露 | 出现同类报错时先查包内文件数是否等于发布包，再决定重装还是改代码 |
| 3 | **loose ref 回退 bug** | `.git/refs/heads/dev/` 目录缺失曾导致 commit「假成功」；09-11 已 `mkdir -p` 根治，需验证本次提交是否真的不再回退 | 提交后必查四项：`git rev-parse HEAD` 是否等于本次短 SHA / `.git/refs/heads/<分支目录>` 是否存在 / `.git/packed-refs` 是否更新；不符则改 packed-refs 并 `git pack-refs --all` 固化 |
| 4 | 审计历史文档保留「四维度」 | `docs/archive/audits/audit-*.md`、`docs/archive/audits/backend-audit-plan-2026-07-17.md` 中「四维度」指**审计方法的四个维度**（链路完整性/边界异常/数据一致性/性能风险），与排课无关；`audit-backend-logic-2026-07-17.md:145` 的「排课四维度」是**当时**的审计结论 | **有意保留**，不要改——改了会破坏历史记录真实性 |
| 5 | 无性能压测数据 | 答辩若被问「支撑多少并发/响应多少毫秒」**没有实测依据** | 现场按预案口径如实说明未做压测，**不要临场编数字** |
| 6 | **前端存在 `status===3`「已取消」的潜在死代码** | 以下位置都写了「`status===3` → 已取消」的渲染/过滤分支，但**后端无任何路径产生该状态、种子数据也没有**，故这些分支永不命中：`BigSchedule.vue:31`、`ScheduleList.vue:28`、`DailyTimeline.vue:102`、`MonthlyCalendar.vue:93`、`WeeklyCalendar.vue:56`、`useScheduleDrag.ts:146`，以及移动端课表各页 | **建议保留**（属防御性写法，删了反而降低健壮性），但需在文档中说明它是**预留态**，避免将来维护者误以为存在「取消 → status=3」的流转。**注意**：`docs/09-接口规范.md:121` 的 `DELETE /api/edu/schedules/{id}` 端点名为「删除」，恰好与逻辑删除的实现一致，无需订正 |
| 7 | **`v1.1.0` 标签引用本地不可解析** | `refs/tags/v1.1.0` → `345799e` 在 packed-refs 中存在、**提交对象也在**（`git log 345799e` 可读），但 `git rev-parse v1.1.0` / `cat-file -t v1.1.0` 均报 unknown revision；`.git/refs/tags/` 为空，目录内有多个 `packed-refs.bak*`。**`git fetch --tags` 未能修复** | 属本仓 ref 异常家族（与 §七-3 同源）。不影响交付物，但会影响按 tag 检出/回退。建议后续执行 `git pack-refs --all` 后复核；取 tag 信息时改用 `git for-each-ref refs/tags/` 或直接 commit SHA |
| 8 | **课程设计报告 docx 内容滞后** | 见 §四-C | 排版阶段另写脚本重生成 |

---

## 八、验收口径备忘

- 测试类计数有两个口径，**都对**，引用时要说清用哪个：**37 个测试源文件**（含基类 `BaseSpringTest`）／**40 份 surefire 报告**（36 个测试类 + `FinanceServiceMockTest` 的 4 个 `@Nested` 内部类）
- 数据库为 **37 张表**（`grep -ciE "^\s*CREATE TABLE" sql/schema.sql`）；项目旧文档/记忆中的「34 表」为**错误值**，已作废
- 「在册学员」=`COUNT(DISTINCT student_id)`（`class_student` 是关联表，`COUNT(*)` 是人次）
